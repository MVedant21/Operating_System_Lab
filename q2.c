#include<stdio.h>

/*Background Process Exploration: Develop a simple program to execute indefinitely in the 
background. Traverse the /proc directory and extract relevant process information from the 
corresponding proc directories.*/

int main(void){
	while(1){ // creating an empty while loop to run the prog infinitely  
	
	}
	return 0;
}
